const randomTextElements = document.querySelectorAll('.randomtekst');
console.log(randomTextElements);

const alleImages = document.querySelectorAll('img');
console.log(alleImages);

const specialeTextElements = document.querySelectorAll('.specialetekst');
console.log(specialeTextElements);

const fifthParagraph = document.querySelectorAll('p')[4];
console.log(fifthParagraph);

const RandomText = document.querySelectorAll('.randomtekst')[1];
console.log(RandomText);
